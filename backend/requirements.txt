# ============================================
# BreizhCrops Crop Classification - Requirements
# ============================================

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Core ML / data
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# BreizhCrops dataset package (time series + parcel geometries)
breizhcrops>=0.0.3.4

# Geospatial handling (parcel polygons, CRS reprojection)
geopandas>=0.13.0
shapely>=2.0.0

# Sentinel-2 L1C time series (Copernicus Data Space Ecosystem, Sentinel Hub
# Statistical API) — server-side per-band zonal statistics, no local raster
# download/clipping needed.
sentinelhub>=3.10.0

# Loads CDSE_SH_CLIENT_ID / CDSE_SH_CLIENT_SECRET from backend/.env (gitignored)
python-dotenv>=1.0.0

# Basemap / satellite tile fetching for visualization
contextily>=1.3.0

# Plotting
matplotlib>=3.7.0

# ============================================
# Notes for deployment:
# - geopandas can be tricky to install outside Colab (GDAL dependency).
#   If pip install fails, try: conda install -c conda-forge geopandas
# - contextily fetches basemap tiles live from Esri's servers (free/anonymous
#   endpoint) - can rate-limit under load. For production, consider a paid
#   tile provider (Mapbox/Esri/Google) with an API key instead.
# - PyTorch: install the CUDA-matched build from https://pytorch.org/get-started/locally/
#   if deploying with GPU inference; the line above installs CPU-only by default
#   via plain pip on most systems.
# ============================================
